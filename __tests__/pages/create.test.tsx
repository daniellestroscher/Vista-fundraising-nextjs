import React from "react";
//TODO GET IPFS-HTTP-CLIENT TO RESOLVE WITH A MOCK.

describe("Create", function () {
  test("renders create component without errors", () => {
    // const mockIpfsClient = mocked(create);
  });
});
